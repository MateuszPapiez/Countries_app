
import SwiftUI

struct CountryInfo: View {
    var body: some View {
        NavigationView{
            Text("ABC")
        }
        .navigationTitle("Country "+"Polska")
    }
}

struct CountryInfo_Previews: PreviewProvider {
    static var previews: some View {
        CountryInfo()
    }
}
